import { style } from '@vanilla-extract/css';

export const containerClass = style({
  display: 'flex',
  gap: '4px',
  marginBottom: '16px',
});

export const titleClass = style({
  fontSize: '1rem',
  margin: 0,
});
