kind: Service
apiVersion: v1
metadata:
  labels:
    name: example-card-custom-service
    env: prod
  name: example-card-custom-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: example-card-custom
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: example-card-custom-deployment
    env: prod
  name: example-card-custom-deployment
spec:
  selector:
    matchLabels:
      app: example-card-custom
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: example-card-custom
    spec:
      containers:
        - name: example-card-custom
          image: KUSTOMIZE_IMAGE
          ports:
            - containerPort: 80
              name: http
          lifecycle:
            preStop:
              exec:
                command: ['/usr/sbin/nginx', '-s', 'quit']
