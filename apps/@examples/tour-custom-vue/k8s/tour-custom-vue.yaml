kind: Service
apiVersion: v1
metadata:
  labels:
    name: example-tour-custom-vue-service
    env: prod
  name: example-tour-custom-vue-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: example-tour-custom-vue
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    name: example-tour-custom-vue-deployment
    env: prod
  name: example-tour-custom-vue-deployment
spec:
  selector:
    matchLabels:
      app: example-tour-custom-vue
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: example-tour-custom-vue
    spec:
      containers:
        - name: example-tour-custom-vue
          image: KUSTOMIZE_IMAGE
          ports:
            - containerPort: 80
              name: http
          lifecycle:
            preStop:
              exec:
                command: ['/usr/sbin/nginx', '-s', 'quit']
