## @dopt/check-formatting

A CLI for checking whether a package's files are formatted correctly.

```
  $ npx check-formatting -h

  Check file formatting relative to cwd

  Usage

    $ check-formatting

    $ check-formatting -s #staged files only

```
